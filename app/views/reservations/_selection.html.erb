<% if @reservation.nil? %>
	<% @reservation=Reservation.new %>
<% end %>
<%= form_for(@reservation, remote: true ) do |f| %>
	<%= f.hidden_field :seat_id, :class => "seat_id"%>
	<%= f.submit "id", class: "btn btn-mini",id:"button", onclick: "getdiv(this)" %>
<%end%>